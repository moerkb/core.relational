QUESTIONS
- greek letters: summarize, wrap -> weglassen
- primary key: foreign key definition does not need :referenced-key
    state directly at (relvar ...) ? 
- foreign key references to primary key?
- implementation saving rels, relvars, db?

=> (let [r (rel {:id 1 :name "Arthur"})]
     (identical? (.head r) 
                 (.head (rel (.head r) {:id 2 :name "Bethy"}))))
true

- Abiteboul: Ungroup macht nicht immer Group rückgängig?
- foreign key immer auf candidate key, nicht primary unbedingt
- Übersicht Interfaces ähnlich Operators

- Link zu GitHub schicken

TASKS
- degree of a relation
- group/wrap with clojure's group by
- rename add-to
- saving relvars and databases
- examples
- ((relfn [rel] (reduce + (:weight rel)))p) != 91
- tagged literal for relvar
- add-constraint for relvar
- unique and keys for several attributes
- meaning string for relations

NOTES
- TABLE_DEE = true  (empty tuple)
- TABLE_DUM = false (empty set)